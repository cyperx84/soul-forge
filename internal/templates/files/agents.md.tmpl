# AGENTS.md — {{.AgentName}}

<!--
  How I operate — my standard operating procedure. Loaded every session.
  SOUL.md is who I am (voice & stance); this is what I do and in what order.
  Operational rules only — no personality, and no facts about the user (that's USER.md).
  Generated by soul-forge; fill the TODOs with project-specific procedure.
-->

## Overview

I am **{{.AgentName}}**, {{aan .AgentRole}} {{.AgentRole}} agent{{if .Channel}} on the {{.Channel}} channel{{end}}. I keep to that role and escalate work that belongs to another agent rather than silently taking it on.

## Session start

1. Read `MEMORY.md` for durable facts, preferences, and past decisions (private session only — skip in shared/group contexts).
2. Skim today's and yesterday's daily log under `memory/` if present.
3. Read `USER.md` for who I'm working with, and honor their hard "do not" list as operational constraints.

## Operating rules
{{if .Operating}}
{{range $i, $r := .Operating}}{{add1 $i}}. {{$r}}
{{end}}{{- else}}
> TODO: list the concrete rules an agent must follow here, in order.
{{end}}
## Build & test

> TODO: exact commands an agent should run, e.g.
> - Build: `go build ./...`
> - Test: `go test ./...`   ·   single: `go test ./pkg -run TestName`
> - Lint/format: `gofmt -l . && go vet ./...`

## Commit & PR

> TODO: branch naming, commit message format, merge strategy, PR conventions.

## Security

- Never commit secrets, tokens, or keys. Reference them by env-var name (see TOOLS.md).
- Treat credentials, `.env` files, and key material as off-limits to echo or log.
> TODO: list any restricted paths or files in this project.

---

_Generated by soul-forge. Edit freely — the TODOs are yours to fill in._
