# TOOLS.md — {{.AgentName}}

<!--
  Notes about local tools, environment, and conventions — a cheat sheet for "where
  things are." This is guidance only; it does NOT control which tools are available.
  NEVER put secrets here — reference env vars by name only.
  Generated by soul-forge from the user's environment + tooling.
-->

## Operating Environment
{{with .Profile.Environment}}
{{if .OS}}- **OS:** {{.OS}}
{{end}}{{if .Shell}}- **Shell:** {{.Shell}}
{{end}}{{if .Editor}}- **Editor:** {{.Editor}}
{{end}}{{if .Terminal}}- **Terminal:** {{.Terminal}}
{{end}}{{if .PackageManager}}- **Package manager:** {{.PackageManager}}
{{end}}{{if .Hardware}}- **Hardware:** {{.Hardware}}
{{end}}
{{- else}}
> TODO: describe the OS, shell, and editor this agent runs in.
{{end}}
{{- with .Profile.Environment}}{{if .KeyTools}}
## Key Tools

{{bullets .KeyTools -}}
{{end}}{{end}}
{{- with .Profile.WorkStyle}}{{if .Tools}}
## Workflow Tools

{{bullets .Tools -}}
{{end}}{{end}}
{{- with .Profile.Environment}}{{if .Aliases}}
## Useful Aliases

{{bullets .Aliases -}}
{{end}}{{end}}

## Where Things Are

> TODO: the "cheat sheet" — API base URLs/endpoints, SSH hosts and aliases, service
> ports, device/host names, paths. Names and locations only, never credentials.

## Secrets & Credentials

- Secrets are referenced by **environment variable name only** — never written here.
- Never echo, log, or commit credentials, tokens, or keys.
> TODO: list the env var names this agent expects (e.g. `GITHUB_TOKEN`, `DATABASE_URL`) — names only, never values.

---

_Generated by soul-forge. Update `.soul-forge/profile.json` (and re-scan dotfiles) then re-run `soul-forge generate`._
